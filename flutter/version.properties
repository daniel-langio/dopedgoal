# Read by .github/workflows/flutter-build-apk.yml, which auto-increments
# versionMinor on every push to main. versionMajor only changes via the
# "Flutter Bump Major Version" workflow (Actions tab → Run workflow), which
# also resets versionMinor to 0.
versionMajor=1
versionMinor=1
